//Font family sans-serif is used because the font family inter which was given in the ass 
//needs to be loaded into my system because my system is bit old so I didnt forced on it becuase it might
//create difficulties 


//Comments are added for every component and function created for better understaing and flow of the code
import './App.css';
import Eden from './components/Eden';

function App() {
  return (
    <div className="App" >
      <Eden />
    </div>
  );
}

export default App;
